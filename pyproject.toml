[project]
name = "webui-siri-shortcut"
version = "0.1.0"
description = "Apple Siri Shortcut bridge for Open WebUI"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "uvicorn[standard]>=0.29.0",
]
